import fs from 'node:fs/promises';
import path from 'node:path';
import { spawnSync } from 'node:child_process';
import { fileURLToPath } from 'node:url';

const here = path.dirname(fileURLToPath(import.meta.url));
const mainPath = path.resolve(here, '..', 'main.js');
const marker = '// userFLOW profile runtime optimization v1';
let source = await fs.readFile(mainPath, 'utf8');

if (!source.includes(marker)) {
  const restoreStart = source.indexOf('async function restoreManagedSession(');
  const restoreEnd = source.indexOf('\nfunction profileLabel(', restoreStart);
  if (restoreStart < 0 || restoreEnd < 0) throw new Error('Could not locate restoreManagedSession() in main.js');

  const optimizedRestore = `${marker}\nlet managedSessionStateCache = null;\nlet managedSessionStateWrite = Promise.resolve();\n\nfunction managedSessionStatePath() {\n  return path.join(app.getPath('userData'), 'profile-session-state.json');\n}\n\nfunction managedSessionStateKey(profile) {\n  const clientId = authMeta?.client?.id || 'client';\n  return \`${'${clientId}'}:${'${profile.id}'}\`;\n}\n\nasync function readManagedSessionState() {\n  if (managedSessionStateCache) return managedSessionStateCache;\n  try {\n    const parsed = JSON.parse(await fs.readFile(managedSessionStatePath(), 'utf8'));\n    managedSessionStateCache = parsed && typeof parsed === 'object' ? parsed : {};\n  } catch {\n    managedSessionStateCache = {};\n  }\n  return managedSessionStateCache;\n}\n\nasync function rememberManagedSessionVersion(profile, version) {\n  if (!version) return;\n  const state = await readManagedSessionState();\n  state[managedSessionStateKey(profile)] = { version: String(version), updatedAt: Date.now() };\n  managedSessionStateWrite = managedSessionStateWrite.then(async () => {\n    await fs.mkdir(path.dirname(managedSessionStatePath()), { recursive: true });\n    await fs.writeFile(managedSessionStatePath(), JSON.stringify(state), { encoding: 'utf8', mode: 0o600 });\n  }).catch(() => null);\n  await managedSessionStateWrite;\n}\n\nfunction normalizedCookieDomain(value) {\n  return String(value || '').trim().replace(/^\\./, '').toLowerCase();\n}\n\nfunction expectedCookiePresent(expected, currentCookies) {\n  const expectedDomain = normalizedCookieDomain(expected.domain);\n  const expectedPath = typeof expected.path === 'string' && expected.path ? expected.path : '/';\n  return currentCookies.some((cookie) => {\n    if (cookie.name !== String(expected.name || '')) return false;\n    if (expectedDomain && normalizedCookieDomain(cookie.domain) !== expectedDomain) return false;\n    const currentPath = typeof cookie.path === 'string' && cookie.path ? cookie.path : '/';\n    return currentPath === expectedPath || expectedPath === '/';\n  });\n}\n\nasync function canReuseManagedSession(browserSession, profile, delivery, material) {\n  const version = delivery?.version == null ? '' : String(delivery.version);\n  if (!version) return false;\n  const state = await readManagedSessionState();\n  if (state?.[managedSessionStateKey(profile)]?.version !== version) return false;\n\n  const now = Date.now() / 1000;\n  const expectedCookies = (Array.isArray(material.cookies) ? material.cookies : []).filter((cookie) => {\n    if (!cookie?.name) return false;\n    return typeof cookie.expirationDate !== 'number' || cookie.expirationDate > now;\n  });\n  if (!expectedCookies.length) return false;\n\n  let currentCookies;\n  try {\n    currentCookies = await browserSession.cookies.get({ url: profile.url });\n  } catch {\n    return false;\n  }\n  if (!currentCookies.length) return false;\n  return expectedCookies.every((cookie) => expectedCookiePresent(cookie, currentCookies));\n}\n\nasync function restoreManagedSession(webContents, browserSession, profile, delivery) {\n  const material = delivery?.material;\n  if (!material || material.format !== 'userflex-browser-session-v1') {\n    throw new UserflexError('El material de sesión del perfil no es compatible.', 'SESSION_MATERIAL_INVALID');\n  }\n  if (material.profileId && material.profileId !== profile.id) {\n    throw new UserflexError('La sesión recibida no pertenecece a este perfil.', 'SESSION_PROFILE_MISMATCH');\n  }\n  const targetOrigin = new URL(profile.url).origin;\n  if (material.allowedOrigin && material.allowedOrigin !== targetOrigin) {\n    throw new UserflexError('El origen de la sesión no coincide con la web del perfil.', 'SESSION_ORIGIN_MISMATCH');\n  }\n\n  // A persistent Electron partition already stores this profile's own cookies,\n  // cache, IndexedDB and localStorage on this Windows PC. When the server is\n  // delivering the same managed-session version and its cookie set is still\n  // present, open the profile directly instead of clearing/rebuilding it.\n  if (await canReuseManagedSession(browserSession, profile, delivery, material)) {\n    await webContents.loadURL(profile.url);\n    return { reusedLocalProfile: true };\n  }\n\n  await browserSession.clearStorageData({ storages: ['cookies'] });\n  const cookies = Array.isArray(material.cookies) ? material.cookies : [];\n  await Promise.allSettled(cookies.filter((cookie) => cookie?.name).map((cookie) =>\n    browserSession.cookies.set(cookieSetPayload(cookie, profile.url))\n  ));\n\n  await webContents.loadURL(profile.url);\n  const storage = material.storage && typeof material.storage === 'object' ? material.storage : null;\n  if (storage) {\n    let currentOrigin = null;\n    try {\n      currentOrigin = new URL(webContents.getURL()).origin;\n    } catch {}\n\n    if (!storage.origin || storage.origin === currentOrigin) {\n      const localStorageData = storage.localStorage && typeof storage.localStorage === 'object' ? storage.localStorage : {};\n      const sessionStorageData = storage.sessionStorage && typeof storage.sessionStorage === 'object' ? storage.sessionStorage : {};\n      const hasStorage = Object.keys(localStorageData).length > 0 || Object.keys(sessionStorageData).length > 0;\n      if (hasStorage) {\n        await webContents.executeJavaScript(\`(() => {\n          const localData = ${'${JSON.stringify(localStorageData)}'};\n          const sessionData = ${'${JSON.stringify(sessionStorageData)}'};\n          try { localStorage.clear(); } catch {}\n          try { sessionStorage.clear(); } catch {}\n          for (const [key, value] of Object.entries(localData)) { try { localStorage.setItem(key, String(value)); } catch {} }\n          for (const [key, value] of Object.entries(sessionData)) { try { sessionStorage.setItem(key, String(value)); } catch {} }\n          return true;\n        })()\`);\n        webContents.reload();\n      }\n    }\n  }\n\n  try { browserSession.flushStorageData(); } catch {}\n  await rememberManagedSessionVersion(profile, delivery?.version);\n  return { reusedLocalProfile: false };\n}\n`;

  source = source.slice(0, restoreStart) + optimizedRestore + source.slice(restoreEnd);

  const eventsStart = source.indexOf('function attachProfilePageEvents(');
  const eventsEnd = source.indexOf('\nfunction createProfilePageView(', eventsStart);
  if (eventsStart < 0 || eventsEnd < 0) throw new Error('Could not locate attachProfilePageEvents() in main.js');

  const optimizedEvents = `function scheduleProfilePageRecovery(workspace, page, delayMs = 650) {\n  if (!workspace || !page || workspace.cleaned || workspace.closing) return;\n  const webContents = page.view?.webContents;\n  if (!webContents || webContents.isDestroyed()) return;\n  if (page.recoveryTimer) return;\n  const attempts = Number(page.recoveryAttempts || 0);\n  if (attempts >= 2) return;\n  page.recoveryAttempts = attempts + 1;\n  page.recoveryTimer = setTimeout(() => {\n    page.recoveryTimer = null;\n    if (workspace.cleaned || workspace.closing || webContents.isDestroyed()) return;\n    try { webContents.reload(); } catch {}\n  }, delayMs * (attempts + 1));\n}\n\nfunction attachProfilePageEvents(workspace, page) {\n  const webContents = page.view.webContents;\n  const update = () => updateWorkspaceState(workspace);\n  webContents.on('did-start-loading', () => {\n    page.loading = true;\n    update();\n  });\n  webContents.on('did-stop-loading', () => {\n    page.loading = false;\n    update();\n  });\n  webContents.on('did-finish-load', () => {\n    page.recoveryAttempts = 0;\n  });\n  webContents.on('did-fail-load', (_event, errorCode, _errorDescription, _validatedURL, isMainFrame) => {\n    if (!isMainFrame || errorCode === -3) return;\n    if ([-2, -7, -21, -105, -106, -118].includes(Number(errorCode))) scheduleProfilePageRecovery(workspace, page);\n  });\n  webContents.on('render-process-gone', (_event, details) => {\n    if (details?.reason === 'clean-exit') return;\n    scheduleProfilePageRecovery(workspace, page, 850);\n  });\n  webContents.on('did-navigate', update);\n  webContents.on('did-navigate-in-page', update);\n  webContents.on('page-title-updated', (_event, title) => {\n    page.title = title || page.title;\n    update();\n  });\n  webContents.setWindowOpenHandler(({ url }) => {\n    try {\n      const target = new URL(url);\n      if (target.protocol !== 'https:' && target.protocol !== 'http:') return { action: 'deny' };\n      if (workspace.detachedWindow && !workspace.detachedWindow.isDestroyed()) {\n        void createProfilePage(workspace, target.toString(), true);\n      } else {\n        void webContents.loadURL(target.toString());\n      }\n    } catch {\n      // Ignore invalid popups.\n    }\n    return { action: 'deny' };\n  });\n}\n`;

  source = source.slice(0, eventsStart) + optimizedEvents + source.slice(eventsEnd);
  await fs.writeFile(mainPath, source, 'utf8');
}

const check = spawnSync(process.execPath, ['--check', mainPath], { stdio: 'inherit' });
if (check.status !== 0) process.exit(check.status || 1);
console.log('userFLOW profile runtime optimization ready.');
