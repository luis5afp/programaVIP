# Configure these as Cloudflare secrets, never commit real values.
SUPABASE_URL="https://lbvxnbbglkjnwphaomyx.supabase.co"
SUPABASE_SERVICE_ROLE_KEY=""

# 32 random bytes encoded as base64url. Used only in the Worker to encrypt proxy passwords.
USERFLEX_PROXY_MASTER_KEY=""

# Public configuration. Defaults to the existing CreatorTools LAB authentication origin.
CREATORTOOLS_AUTH_ORIGIN="https://creatortools-reconstruction-lab.luis5afp.workers.dev"
USERFLEX_PROXY_KEY_VERSION="v1"
